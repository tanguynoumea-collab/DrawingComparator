<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=System.Runtime">

    <!-- ═══ Couleurs (DESIGN_PLAN §2 — colonne LIGHT ; chrome seul, le canvas reste blanc) ═══
         DUPLIQUÉ VOLONTAIREMENT avec Tokens.xaml (les StaticResource des vues se résolvent
         au chargement : un seul des deux fichiers est mergé, choisi au démarrage — SEN-13).
         Toute retouche de token doit être reportée dans LES DEUX fichiers. -->
    <Color x:Key="ColorBackground">#F2F3F5</Color>
    <Color x:Key="ColorSurface">#FFFFFF</Color>
    <Color x:Key="ColorTextPrimary">#1B1D23</Color>
    <Color x:Key="ColorTextSecondary">#5F6570</Color>
    <Color x:Key="ColorAccent">#B8860B</Color>
    <Color x:Key="ColorAccentText">#FFFFFF</Color>
    <Color x:Key="ColorDanger">#C62828</Color>

    <!-- Couleurs métier : les teintes des plans. Constantes du domaine, identiques dans les deux thèmes. -->
    <Color x:Key="ColorTintBase">#D32F2F</Color>
    <Color x:Key="ColorTintRevision">#1E5AC8</Color>

    <SolidColorBrush x:Key="BrushBackground" Color="{StaticResource ColorBackground}" />
    <SolidColorBrush x:Key="BrushSurface" Color="{StaticResource ColorSurface}" />
    <SolidColorBrush x:Key="BrushTextPrimary" Color="{StaticResource ColorTextPrimary}" />
    <SolidColorBrush x:Key="BrushTextSecondary" Color="{StaticResource ColorTextSecondary}" />
    <SolidColorBrush x:Key="BrushAccent" Color="{StaticResource ColorAccent}" />
    <SolidColorBrush x:Key="BrushAccentText" Color="{StaticResource ColorAccentText}" />
    <SolidColorBrush x:Key="BrushDanger" Color="{StaticResource ColorDanger}" />
    <SolidColorBrush x:Key="BrushTintBase" Color="{StaticResource ColorTintBase}" />
    <SolidColorBrush x:Key="BrushTintRevision" Color="{StaticResource ColorTintRevision}" />

    <!-- ═══ Espacement : grille de 4 px (PERT-07 : seuls les crans consommés) ═══ -->
    <Thickness x:Key="SpaceM">16</Thickness>
    <Thickness x:Key="SpaceL">24</Thickness>

    <!-- ═══ Typographie ═══ -->
    <sys:Double x:Key="FontSizeCaption">12</sys:Double>
    <sys:Double x:Key="FontSizeBody">14</sys:Double>
    <sys:Double x:Key="FontSizeSubtitle">18</sys:Double>
    <sys:Double x:Key="FontSizeData">13</sys:Double>
    <FontFamily x:Key="FontData">Cascadia Mono, Consolas</FontFamily>

    <!-- ═══ Formes ═══ -->
    <CornerRadius x:Key="RadiusControl">4</CornerRadius>
    <CornerRadius x:Key="RadiusCard">8</CornerRadius>

    <!-- ═══ Styles dérivés ═══ -->
    <Style x:Key="PanelTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource FontSizeCaption}" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextSecondary}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Typography.Capitals" Value="AllSmallCaps" />
    </Style>

    <Style x:Key="DataText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource FontData}" />
        <Setter Property="FontSize" Value="{StaticResource FontSizeData}" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextSecondary}" />
    </Style>
</ResourceDictionary>
