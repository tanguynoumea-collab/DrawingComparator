<Window x:Class="DrawingComparator.App.Views.ExportProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export en cours"
        Width="380"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BrushBackground}"
        Foreground="{StaticResource BrushTextPrimary}"
        WindowStyle="ToolWindow"
        Closing="OnClosing">
    <StackPanel Margin="{StaticResource SpaceL}">
        <TextBlock Text="EXPORT DE LA FEUILLE"
                   Style="{StaticResource PanelTitle}"
                   Margin="0,0,0,8" />
        <!-- Progression déterminée : 1 tick par bande rendue (DESIGN_PLAN §11.5).
             La grille du dialogue ne bouge pas pendant la progression. -->
        <ProgressBar x:Name="Bar"
                     Minimum="0"
                     Maximum="100"
                     Height="8"
                     Foreground="{StaticResource BrushAccent}" />
        <TextBlock x:Name="PercentText"
                   Style="{StaticResource DataText}"
                   Text="0 %"
                   Margin="0,8,0,0" />
        <Button x:Name="CancelButton"
                Content="Annuler"
                MinWidth="88"
                MinHeight="32"
                HorizontalAlignment="Right"
                Margin="0,16,0,0"
                Click="OnCancel" />
    </StackPanel>
</Window>
